events {
    worker_connections 1024;
}

http {
    upstream flask_app {
        server web:5000;
    }

    # Docker DNS; variable-based proxying keeps Power BI available if AI is restarting.
    resolver 127.0.0.11 valid=10s ipv6=off;

    # HTTP — certbot challenge + redirect to HTTPS
    server {
        listen 80;
        server_name powerbi.mozaicweb.com;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # AI assistant HTTP — certbot challenge + redirect to HTTPS
    server {
        listen 80;
        server_name askai.mozaicweb.com;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS
    server {
        listen 443 ssl;
        server_name powerbi.mozaicweb.com;

        ssl_certificate     /etc/letsencrypt/live/powerbi.mozaicweb.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/powerbi.mozaicweb.com/privkey.pem;

        ssl_protocols             TLSv1.2 TLSv1.3;
        ssl_ciphers               ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache         shared:SSL:10m;
        ssl_session_timeout       1d;

        client_max_body_size 20M;

        location / {
            proxy_pass            http://flask_app;
            proxy_set_header      Host              $host;
            proxy_set_header      X-Real-IP         $remote_addr;
            proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header      X-Forwarded-Proto $scheme;
            proxy_read_timeout    120s;
            proxy_connect_timeout 10s;
        }
    }

    # AI assistant HTTPS
    server {
        listen 443 ssl;
        server_name askai.mozaicweb.com;

        ssl_certificate     /etc/letsencrypt/live/askai.mozaicweb.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/askai.mozaicweb.com/privkey.pem;

        ssl_protocols             TLSv1.2 TLSv1.3;
        ssl_ciphers               ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_cache         shared:SSL:10m;
        ssl_session_timeout       1d;

        client_max_body_size 2M;

        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header Referrer-Policy no-referrer always;

        location / {
            set                   $ai_upstream http://ai_frontend;
            proxy_pass            $ai_upstream;
            proxy_set_header      Host              $host;
            proxy_set_header      X-Real-IP         $remote_addr;
            proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header      X-Forwarded-Proto $scheme;
            proxy_read_timeout    180s;
            proxy_connect_timeout 10s;
        }
    }
}
